<p id="notice"><%= notice %></p>

<h1>Leads</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Name</th>
      <th>Birth at</th>
      <th>Phone</th>
      <th>Address</th>
      <th>Franchise</th>
      <th>Encrypted credit card number</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @leads.each do |lead| %>
      <tr>
        <td><%= lead.user_id %></td>
        <td><%= lead.name %></td>
        <td><%= lead.birth_at %></td>
        <td><%= lead.phone %></td>
        <td><%= lead.address %></td>
        <td><%= lead.franchise %></td>
        <td><%= lead.encrypted_credit_card_number %></td>
        <td><%= lead.email %></td>
        <td><%= link_to 'Show', lead %></td>
        <td><%= link_to 'Edit', edit_lead_path(lead) %></td>
        <td><%= link_to 'Destroy', lead, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lead', new_lead_path %>
